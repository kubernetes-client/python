# coding: utf-8

"""
    Kubernetes

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: release-1.36
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from collections.abc import Mapping as _Mapping
from datetime import datetime
from pydantic import AliasChoices, BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, cast as _cast
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


_OPENAPI_GENERATOR_TO_DICT = "_openapi_generator_to_dict"


def _get_openapi_to_dict(value: Any) -> Any:
    # Reciprocal function references preserve inherited generated methods
    # without reserving model member names. Checking both directions also
    # rejects overrides whose decorators copy function attributes.
    to_dict = getattr(value, "to_dict", None)
    type_to_dict = getattr(type(value), "to_dict", None)
    if (
        not callable(to_dict)
        or getattr(to_dict, "__func__", None) is not type_to_dict
    ):
        return None

    openapi_to_dict = getattr(
        type_to_dict, _OPENAPI_GENERATOR_TO_DICT, None
    )
    if (
        not callable(openapi_to_dict)
        or getattr(
            openapi_to_dict,
            _OPENAPI_GENERATOR_TO_DICT,
            None,
        ) is not type_to_dict
    ):
        return None
    return openapi_to_dict


def _to_legacy_item(value: Any, serialize: bool) -> Any:
    to_dict = getattr(value, "to_dict", None)
    if _get_openapi_to_dict(value) is not None and callable(to_dict):
        return to_dict(serialize=serialize)
    if callable(to_dict):
        return to_dict()
    return value


def _to_legacy_value(value: Any, serialize: bool) -> Any:
    # python-legacy converted only immediate list elements or dictionary values:
    # https://github.com/OpenAPITools/openapi-generator/blob/c84b949df1a9ec04ba75989cb49b45c940c2f694/modules/openapi-generator/src/main/resources/python-legacy/model.mustache#L203-L231
    if isinstance(value, list):
        return [_to_legacy_item(item, serialize) for item in value]
    if isinstance(value, dict):
        return {
            key: _to_legacy_item(item, serialize)
            for key, item in value.items()
        }
    return _to_legacy_item(value, serialize)


def _to_openapi_value(value: Any) -> Any:
    if isinstance(value, list):
        return [_to_openapi_value(item) for item in value]
    if isinstance(value, dict):
        return {key: _to_openapi_value(item) for key, item in value.items()}

    to_openapi_dict = _get_openapi_to_dict(value)
    if to_openapi_dict is not None:
        return to_openapi_dict(value)

    to_dict = getattr(value, "to_dict", None)
    if callable(to_dict):
        return to_dict()
    return value


class V1beta1LeaseCandidateSpec(BaseModel):
    """
    LeaseCandidateSpec is a specification of a Lease.

    Attributes:
      openapi_types (dict): The key is attribute name
                            and the value is attribute type.
      attribute_map (dict): The key is attribute name
                            and the value is json key in definition.
    """ # noqa: E501
    binary_version: StrictStr = Field(validation_alias=AliasChoices("binaryVersion", "binary_version"), serialization_alias="binaryVersion", description="BinaryVersion is the binary version. It must be in a semver format without leading `v`. This field is required.")
    emulation_version: Optional[StrictStr] = Field(default=None, validation_alias=AliasChoices("emulationVersion", "emulation_version"), serialization_alias="emulationVersion", description="EmulationVersion is the emulation version. It must be in a semver format without leading `v`. EmulationVersion must be less than or equal to BinaryVersion. This field is required when strategy is \"OldestEmulationVersion\"")
    lease_name: StrictStr = Field(validation_alias=AliasChoices("leaseName", "lease_name"), serialization_alias="leaseName", description="LeaseName is the name of the lease for which this candidate is contending. The limits on this field are the same as on Lease.name. Multiple lease candidates may reference the same Lease.name. This field is immutable.")
    ping_time: Optional[datetime] = Field(default=None, validation_alias=AliasChoices("pingTime", "ping_time"), serialization_alias="pingTime", description="PingTime is the last time that the server has requested the LeaseCandidate to renew. It is only done during leader election to check if any LeaseCandidates have become ineligible. When PingTime is updated, the LeaseCandidate will respond by updating RenewTime.")
    renew_time: Optional[datetime] = Field(default=None, validation_alias=AliasChoices("renewTime", "renew_time"), serialization_alias="renewTime", description="RenewTime is the time that the LeaseCandidate was last updated. Any time a Lease needs to do leader election, the PingTime field is updated to signal to the LeaseCandidate that they should update the RenewTime. Old LeaseCandidate objects are also garbage collected if it has been hours since the last renew. The PingTime field is updated regularly to prevent garbage collection for still active LeaseCandidates.")
    strategy: StrictStr = Field(description="Strategy is the strategy that coordinated leader election will use for picking the leader. If multiple candidates for the same Lease return different strategies, the strategy provided by the candidate with the latest BinaryVersion will be used. If there is still conflict, this is a user error and coordinated leader election will not operate the Lease until resolved.")
    openapi_types: ClassVar[Dict[str, str]] = {
        "binary_version": "str",
        "emulation_version": "str",
        "lease_name": "str",
        "ping_time": "datetime",
        "renew_time": "datetime",
        "strategy": "str"
    }

    attribute_map: ClassVar[Dict[str, str]] = {
        "binary_version": "binaryVersion",
        "emulation_version": "emulationVersion",
        "lease_name": "leaseName",
        "ping_time": "pingTime",
        "renew_time": "renewTime",
        "strategy": "strategy"
    }
    __properties: ClassVar[List[str]] = ["binaryVersion", "emulationVersion", "leaseName", "pingTime", "renewTime", "strategy"]

    @classmethod
    def __preprocess_input_names(
        cls,
        obj: Any,
        remove_hidden_storage_names: bool = True,
    ) -> Any:
        if not isinstance(obj, _Mapping):
            return obj
        obj = dict(obj)
        if "binaryVersion" not in obj and "binary_version" in obj:
            obj["binaryVersion"] = obj["binary_version"]
        obj.pop("binary_version", None)
        if "emulationVersion" not in obj and "emulation_version" in obj:
            obj["emulationVersion"] = obj["emulation_version"]
        obj.pop("emulation_version", None)
        if "leaseName" not in obj and "lease_name" in obj:
            obj["leaseName"] = obj["lease_name"]
        obj.pop("lease_name", None)
        if "pingTime" not in obj and "ping_time" in obj:
            obj["pingTime"] = obj["ping_time"]
        obj.pop("ping_time", None)
        if "renewTime" not in obj and "renew_time" in obj:
            obj["renewTime"] = obj["renew_time"]
        obj.pop("renew_time", None)
        return obj

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        extra="forbid",
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model"""
        return pprint.pformat(self.to_dict())

    def __repr__(self) -> str:
        """For print and pprint"""
        return self.to_str()

    def __eq__(self, other: object) -> bool:
        """Returns true if both objects are equal"""
        if not isinstance(other, V1beta1LeaseCandidateSpec):
            return False

        return self.to_dict() == other.to_dict()

    def __ne__(self, other: object) -> bool:
        """Returns true if both objects are not equal"""
        if not isinstance(other, V1beta1LeaseCandidateSpec):
            return True

        return not (self == other)


    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        to_openapi_dict = _get_openapi_to_dict(self)
        if to_openapi_dict is not None:
            return json.dumps(to_jsonable_python(to_openapi_dict(self)))
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of V1beta1LeaseCandidateSpec from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self, serialize: bool = False) -> Dict[str, Any]:
        """Return all declared model fields using public or wire names."""
        return {
            ("binaryVersion" if serialize else "binary_version"): _to_legacy_value(getattr(self, "binary_version", None), serialize),
            ("emulationVersion" if serialize else "emulation_version"): _to_legacy_value(getattr(self, "emulation_version", None), serialize),
            ("leaseName" if serialize else "lease_name"): _to_legacy_value(getattr(self, "lease_name", None), serialize),
            ("pingTime" if serialize else "ping_time"): _to_legacy_value(getattr(self, "ping_time", None), serialize),
            ("renewTime" if serialize else "renew_time"): _to_legacy_value(getattr(self, "renew_time", None), serialize),
            ("strategy" if serialize else "strategy"): _to_legacy_value(getattr(self, "strategy", None), serialize),
        }

    def __openapi_generator_modern_projection(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    setattr(
        to_dict,
        _OPENAPI_GENERATOR_TO_DICT,
        __openapi_generator_modern_projection,
    )
    setattr(
        __openapi_generator_modern_projection,
        _OPENAPI_GENERATOR_TO_DICT,
        to_dict,
    )
    del __openapi_generator_modern_projection

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of V1beta1LeaseCandidateSpec from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        obj = _cast(
            Dict[str, Any],
            cls.__preprocess_input_names(
                obj,
                remove_hidden_storage_names=True,
            ),
        )

        _obj = cls.model_validate({
            "binaryVersion": obj.get("binaryVersion"),
            "emulationVersion": obj.get("emulationVersion"),
            "leaseName": obj.get("leaseName"),
            "pingTime": obj.get("pingTime"),
            "renewTime": obj.get("renewTime"),
            "strategy": obj.get("strategy")
        })
        return _obj
