# coding: utf-8

"""
    Kubernetes

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: release-1.36
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from collections.abc import Mapping as _Mapping
from pydantic import AliasChoices, BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, cast as _cast
from kubernetes.client.models.v1beta2_device_toleration import V1beta2DeviceToleration
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


_OPENAPI_GENERATOR_TO_DICT = "_openapi_generator_to_dict"


def _get_openapi_to_dict(value: Any) -> Any:
    # Reciprocal function references preserve inherited generated methods
    # without reserving model member names. Checking both directions also
    # rejects overrides whose decorators copy function attributes.
    to_dict = getattr(value, "to_dict", None)
    type_to_dict = getattr(type(value), "to_dict", None)
    if (
        not callable(to_dict)
        or getattr(to_dict, "__func__", None) is not type_to_dict
    ):
        return None

    openapi_to_dict = getattr(
        type_to_dict, _OPENAPI_GENERATOR_TO_DICT, None
    )
    if (
        not callable(openapi_to_dict)
        or getattr(
            openapi_to_dict,
            _OPENAPI_GENERATOR_TO_DICT,
            None,
        ) is not type_to_dict
    ):
        return None
    return openapi_to_dict


def _to_legacy_item(value: Any, serialize: bool) -> Any:
    to_dict = getattr(value, "to_dict", None)
    if _get_openapi_to_dict(value) is not None and callable(to_dict):
        return to_dict(serialize=serialize)
    if callable(to_dict):
        return to_dict()
    return value


def _to_legacy_value(value: Any, serialize: bool) -> Any:
    # python-legacy converted only immediate list elements or dictionary values:
    # https://github.com/OpenAPITools/openapi-generator/blob/c84b949df1a9ec04ba75989cb49b45c940c2f694/modules/openapi-generator/src/main/resources/python-legacy/model.mustache#L203-L231
    if isinstance(value, list):
        return [_to_legacy_item(item, serialize) for item in value]
    if isinstance(value, dict):
        return {
            key: _to_legacy_item(item, serialize)
            for key, item in value.items()
        }
    return _to_legacy_item(value, serialize)


def _to_openapi_value(value: Any) -> Any:
    if isinstance(value, list):
        return [_to_openapi_value(item) for item in value]
    if isinstance(value, dict):
        return {key: _to_openapi_value(item) for key, item in value.items()}

    to_openapi_dict = _get_openapi_to_dict(value)
    if to_openapi_dict is not None:
        return to_openapi_dict(value)

    to_dict = getattr(value, "to_dict", None)
    if callable(to_dict):
        return to_dict()
    return value


class V1beta2DeviceRequestAllocationResult(BaseModel):
    """
    DeviceRequestAllocationResult contains the allocation result for one request.

    Attributes:
      openapi_types (dict): The key is attribute name
                            and the value is attribute type.
      attribute_map (dict): The key is attribute name
                            and the value is json key in definition.
    """ # noqa: E501
    admin_access: Optional[StrictBool] = Field(default=None, validation_alias=AliasChoices("adminAccess", "admin_access"), serialization_alias="adminAccess", description="AdminAccess indicates that this device was allocated for administrative access. See the corresponding request field for a definition of mode.  This is an alpha field and requires enabling the DRAAdminAccess feature gate. Admin access is disabled if this field is unset or set to false, otherwise it is enabled.")
    binding_conditions: Optional[List[StrictStr]] = Field(default=None, validation_alias=AliasChoices("bindingConditions", "binding_conditions"), serialization_alias="bindingConditions", description="BindingConditions contains a copy of the BindingConditions from the corresponding ResourceSlice at the time of allocation.  This is a beta field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates.")
    binding_failure_conditions: Optional[List[StrictStr]] = Field(default=None, validation_alias=AliasChoices("bindingFailureConditions", "binding_failure_conditions"), serialization_alias="bindingFailureConditions", description="BindingFailureConditions contains a copy of the BindingFailureConditions from the corresponding ResourceSlice at the time of allocation.  This is a beta field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates.")
    consumed_capacity: Optional[Dict[str, StrictStr]] = Field(default=None, validation_alias=AliasChoices("consumedCapacity", "consumed_capacity"), serialization_alias="consumedCapacity", description="ConsumedCapacity tracks the amount of capacity consumed per device as part of the claim request. The consumed amount may differ from the requested amount: it is rounded up to the nearest valid value based on the device’s requestPolicy if applicable (i.e., may not be less than the requested amount).  The total consumed capacity for each device must not exceed the DeviceCapacity's Value.  This field is populated only for devices that allow multiple allocations. All capacity entries are included, even if the consumed amount is zero.")
    device: StrictStr = Field(description="Device references one device instance via its name in the driver's resource pool. It must be a DNS label.")
    driver: StrictStr = Field(description="Driver specifies the name of the DRA driver whose kubelet plugin should be invoked to process the allocation once the claim is needed on a node.  Must be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver. It should use only lower case characters.")
    pool: StrictStr = Field(description="This name together with the driver name and the device name field identify which device was allocated (`<driver name>/<pool name>/<device name>`).  Must not be longer than 253 characters and may contain one or more DNS sub-domains separated by slashes.")
    request: StrictStr = Field(description="Request is the name of the request in the claim which caused this device to be allocated. If it references a subrequest in the firstAvailable list on a DeviceRequest, this field must include both the name of the main request and the subrequest using the format <main request>/<subrequest>.  Multiple devices may have been allocated per request.")
    share_id: Optional[StrictStr] = Field(default=None, validation_alias=AliasChoices("shareID", "share_id"), serialization_alias="shareID", description="ShareID uniquely identifies an individual allocation share of the device, used when the device supports multiple simultaneous allocations. It serves as an additional map key to differentiate concurrent shares of the same device.")
    tolerations: Optional[List[V1beta2DeviceToleration]] = Field(default=None, description="A copy of all tolerations specified in the request at the time when the device got allocated.  The maximum number of tolerations is 16.  This is a beta field and requires enabling the DRADeviceTaints feature gate.")
    openapi_types: ClassVar[Dict[str, str]] = {
        "admin_access": "bool",
        "binding_conditions": "List[str]",
        "binding_failure_conditions": "List[str]",
        "consumed_capacity": "Dict[str, str]",
        "device": "str",
        "driver": "str",
        "pool": "str",
        "request": "str",
        "share_id": "str",
        "tolerations": "List[V1beta2DeviceToleration]"
    }

    attribute_map: ClassVar[Dict[str, str]] = {
        "admin_access": "adminAccess",
        "binding_conditions": "bindingConditions",
        "binding_failure_conditions": "bindingFailureConditions",
        "consumed_capacity": "consumedCapacity",
        "device": "device",
        "driver": "driver",
        "pool": "pool",
        "request": "request",
        "share_id": "shareID",
        "tolerations": "tolerations"
    }
    __properties: ClassVar[List[str]] = ["adminAccess", "bindingConditions", "bindingFailureConditions", "consumedCapacity", "device", "driver", "pool", "request", "shareID", "tolerations"]

    @classmethod
    def __preprocess_input_names(
        cls,
        obj: Any,
        remove_hidden_storage_names: bool = True,
    ) -> Any:
        if not isinstance(obj, _Mapping):
            return obj
        obj = dict(obj)
        if "adminAccess" not in obj and "admin_access" in obj:
            obj["adminAccess"] = obj["admin_access"]
        obj.pop("admin_access", None)
        if "bindingConditions" not in obj and "binding_conditions" in obj:
            obj["bindingConditions"] = obj["binding_conditions"]
        obj.pop("binding_conditions", None)
        if "bindingFailureConditions" not in obj and "binding_failure_conditions" in obj:
            obj["bindingFailureConditions"] = obj["binding_failure_conditions"]
        obj.pop("binding_failure_conditions", None)
        if "consumedCapacity" not in obj and "consumed_capacity" in obj:
            obj["consumedCapacity"] = obj["consumed_capacity"]
        obj.pop("consumed_capacity", None)
        if "shareID" not in obj and "share_id" in obj:
            obj["shareID"] = obj["share_id"]
        obj.pop("share_id", None)
        return obj

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        extra="forbid",
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model"""
        return pprint.pformat(self.to_dict())

    def __repr__(self) -> str:
        """For print and pprint"""
        return self.to_str()

    def __eq__(self, other: object) -> bool:
        """Returns true if both objects are equal"""
        if not isinstance(other, V1beta2DeviceRequestAllocationResult):
            return False

        return self.to_dict() == other.to_dict()

    def __ne__(self, other: object) -> bool:
        """Returns true if both objects are not equal"""
        if not isinstance(other, V1beta2DeviceRequestAllocationResult):
            return True

        return not (self == other)


    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        to_openapi_dict = _get_openapi_to_dict(self)
        if to_openapi_dict is not None:
            return json.dumps(to_jsonable_python(to_openapi_dict(self)))
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of V1beta2DeviceRequestAllocationResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self, serialize: bool = False) -> Dict[str, Any]:
        """Return all declared model fields using public or wire names."""
        return {
            ("adminAccess" if serialize else "admin_access"): _to_legacy_value(getattr(self, "admin_access", None), serialize),
            ("bindingConditions" if serialize else "binding_conditions"): _to_legacy_value(getattr(self, "binding_conditions", None), serialize),
            ("bindingFailureConditions" if serialize else "binding_failure_conditions"): _to_legacy_value(getattr(self, "binding_failure_conditions", None), serialize),
            ("consumedCapacity" if serialize else "consumed_capacity"): _to_legacy_value(getattr(self, "consumed_capacity", None), serialize),
            ("device" if serialize else "device"): _to_legacy_value(getattr(self, "device", None), serialize),
            ("driver" if serialize else "driver"): _to_legacy_value(getattr(self, "driver", None), serialize),
            ("pool" if serialize else "pool"): _to_legacy_value(getattr(self, "pool", None), serialize),
            ("request" if serialize else "request"): _to_legacy_value(getattr(self, "request", None), serialize),
            ("shareID" if serialize else "share_id"): _to_legacy_value(getattr(self, "share_id", None), serialize),
            ("tolerations" if serialize else "tolerations"): _to_legacy_value(getattr(self, "tolerations", None), serialize),
        }

    def __openapi_generator_modern_projection(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in tolerations (list)
        _items = []
        if self.tolerations:
            for _item_tolerations in self.tolerations:
                if _item_tolerations:
                    _items.append(_to_openapi_value(_item_tolerations))
            _dict['tolerations'] = _items
        return _dict

    setattr(
        to_dict,
        _OPENAPI_GENERATOR_TO_DICT,
        __openapi_generator_modern_projection,
    )
    setattr(
        __openapi_generator_modern_projection,
        _OPENAPI_GENERATOR_TO_DICT,
        to_dict,
    )
    del __openapi_generator_modern_projection

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of V1beta2DeviceRequestAllocationResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        obj = _cast(
            Dict[str, Any],
            cls.__preprocess_input_names(
                obj,
                remove_hidden_storage_names=True,
            ),
        )

        _obj = cls.model_validate({
            "adminAccess": obj.get("adminAccess"),
            "bindingConditions": obj.get("bindingConditions"),
            "bindingFailureConditions": obj.get("bindingFailureConditions"),
            "consumedCapacity": obj.get("consumedCapacity"),
            "device": obj.get("device"),
            "driver": obj.get("driver"),
            "pool": obj.get("pool"),
            "request": obj.get("request"),
            "shareID": obj.get("shareID"),
            "tolerations": [V1beta2DeviceToleration.from_dict(_item) for _item in obj["tolerations"]] if obj.get("tolerations") is not None else None
        })
        return _obj
