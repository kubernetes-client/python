# coding: utf-8

"""
    Kubernetes

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: release-1.36
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from collections.abc import Mapping as _Mapping
from datetime import datetime
from pydantic import AliasChoices, BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, cast as _cast
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


_OPENAPI_GENERATOR_TO_DICT = "_openapi_generator_to_dict"


def _get_openapi_to_dict(value: Any) -> Any:
    # Reciprocal function references preserve inherited generated methods
    # without reserving model member names. Checking both directions also
    # rejects overrides whose decorators copy function attributes.
    to_dict = getattr(value, "to_dict", None)
    type_to_dict = getattr(type(value), "to_dict", None)
    if (
        not callable(to_dict)
        or getattr(to_dict, "__func__", None) is not type_to_dict
    ):
        return None

    openapi_to_dict = getattr(
        type_to_dict, _OPENAPI_GENERATOR_TO_DICT, None
    )
    if (
        not callable(openapi_to_dict)
        or getattr(
            openapi_to_dict,
            _OPENAPI_GENERATOR_TO_DICT,
            None,
        ) is not type_to_dict
    ):
        return None
    return openapi_to_dict


def _to_legacy_item(value: Any, serialize: bool) -> Any:
    to_dict = getattr(value, "to_dict", None)
    if _get_openapi_to_dict(value) is not None and callable(to_dict):
        return to_dict(serialize=serialize)
    if callable(to_dict):
        return to_dict()
    return value


def _to_legacy_value(value: Any, serialize: bool) -> Any:
    # python-legacy converted only immediate list elements or dictionary values:
    # https://github.com/OpenAPITools/openapi-generator/blob/c84b949df1a9ec04ba75989cb49b45c940c2f694/modules/openapi-generator/src/main/resources/python-legacy/model.mustache#L203-L231
    if isinstance(value, list):
        return [_to_legacy_item(item, serialize) for item in value]
    if isinstance(value, dict):
        return {
            key: _to_legacy_item(item, serialize)
            for key, item in value.items()
        }
    return _to_legacy_item(value, serialize)


def _to_openapi_value(value: Any) -> Any:
    if isinstance(value, list):
        return [_to_openapi_value(item) for item in value]
    if isinstance(value, dict):
        return {key: _to_openapi_value(item) for key, item in value.items()}

    to_openapi_dict = _get_openapi_to_dict(value)
    if to_openapi_dict is not None:
        return to_openapi_dict(value)

    to_dict = getattr(value, "to_dict", None)
    if callable(to_dict):
        return to_dict()
    return value


class V1LeaseSpec(BaseModel):
    """
    LeaseSpec is a specification of a Lease.

    Attributes:
      openapi_types (dict): The key is attribute name
                            and the value is attribute type.
      attribute_map (dict): The key is attribute name
                            and the value is json key in definition.
    """ # noqa: E501
    acquire_time: Optional[datetime] = Field(default=None, validation_alias=AliasChoices("acquireTime", "acquire_time"), serialization_alias="acquireTime", description="acquireTime is a time when the current lease was acquired.")
    holder_identity: Optional[StrictStr] = Field(default=None, validation_alias=AliasChoices("holderIdentity", "holder_identity"), serialization_alias="holderIdentity", description="holderIdentity contains the identity of the holder of a current lease. If Coordinated Leader Election is used, the holder identity must be equal to the elected LeaseCandidate.metadata.name field.")
    lease_duration_seconds: Optional[StrictInt] = Field(default=None, validation_alias=AliasChoices("leaseDurationSeconds", "lease_duration_seconds"), serialization_alias="leaseDurationSeconds", description="leaseDurationSeconds is a duration that candidates for a lease need to wait to force acquire it. This is measured against the time of last observed renewTime.")
    lease_transitions: Optional[StrictInt] = Field(default=None, validation_alias=AliasChoices("leaseTransitions", "lease_transitions"), serialization_alias="leaseTransitions", description="leaseTransitions is the number of transitions of a lease between holders.")
    preferred_holder: Optional[StrictStr] = Field(default=None, validation_alias=AliasChoices("preferredHolder", "preferred_holder"), serialization_alias="preferredHolder", description="PreferredHolder signals to a lease holder that the lease has a more optimal holder and should be given up. This field can only be set if Strategy is also set.")
    renew_time: Optional[datetime] = Field(default=None, validation_alias=AliasChoices("renewTime", "renew_time"), serialization_alias="renewTime", description="renewTime is a time when the current holder of a lease has last updated the lease.")
    strategy: Optional[StrictStr] = Field(default=None, description="Strategy indicates the strategy for picking the leader for coordinated leader election. If the field is not specified, there is no active coordination for this lease. (Alpha) Using this field requires the CoordinatedLeaderElection feature gate to be enabled.")
    openapi_types: ClassVar[Dict[str, str]] = {
        "acquire_time": "datetime",
        "holder_identity": "str",
        "lease_duration_seconds": "int",
        "lease_transitions": "int",
        "preferred_holder": "str",
        "renew_time": "datetime",
        "strategy": "str"
    }

    attribute_map: ClassVar[Dict[str, str]] = {
        "acquire_time": "acquireTime",
        "holder_identity": "holderIdentity",
        "lease_duration_seconds": "leaseDurationSeconds",
        "lease_transitions": "leaseTransitions",
        "preferred_holder": "preferredHolder",
        "renew_time": "renewTime",
        "strategy": "strategy"
    }
    __properties: ClassVar[List[str]] = ["acquireTime", "holderIdentity", "leaseDurationSeconds", "leaseTransitions", "preferredHolder", "renewTime", "strategy"]

    @classmethod
    def __preprocess_input_names(
        cls,
        obj: Any,
        remove_hidden_storage_names: bool = True,
    ) -> Any:
        if not isinstance(obj, _Mapping):
            return obj
        obj = dict(obj)
        if "acquireTime" not in obj and "acquire_time" in obj:
            obj["acquireTime"] = obj["acquire_time"]
        obj.pop("acquire_time", None)
        if "holderIdentity" not in obj and "holder_identity" in obj:
            obj["holderIdentity"] = obj["holder_identity"]
        obj.pop("holder_identity", None)
        if "leaseDurationSeconds" not in obj and "lease_duration_seconds" in obj:
            obj["leaseDurationSeconds"] = obj["lease_duration_seconds"]
        obj.pop("lease_duration_seconds", None)
        if "leaseTransitions" not in obj and "lease_transitions" in obj:
            obj["leaseTransitions"] = obj["lease_transitions"]
        obj.pop("lease_transitions", None)
        if "preferredHolder" not in obj and "preferred_holder" in obj:
            obj["preferredHolder"] = obj["preferred_holder"]
        obj.pop("preferred_holder", None)
        if "renewTime" not in obj and "renew_time" in obj:
            obj["renewTime"] = obj["renew_time"]
        obj.pop("renew_time", None)
        return obj

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        extra="forbid",
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model"""
        return pprint.pformat(self.to_dict())

    def __repr__(self) -> str:
        """For print and pprint"""
        return self.to_str()

    def __eq__(self, other: object) -> bool:
        """Returns true if both objects are equal"""
        if not isinstance(other, V1LeaseSpec):
            return False

        return self.to_dict() == other.to_dict()

    def __ne__(self, other: object) -> bool:
        """Returns true if both objects are not equal"""
        if not isinstance(other, V1LeaseSpec):
            return True

        return not (self == other)


    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        to_openapi_dict = _get_openapi_to_dict(self)
        if to_openapi_dict is not None:
            return json.dumps(to_jsonable_python(to_openapi_dict(self)))
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of V1LeaseSpec from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self, serialize: bool = False) -> Dict[str, Any]:
        """Return all declared model fields using public or wire names."""
        return {
            ("acquireTime" if serialize else "acquire_time"): _to_legacy_value(getattr(self, "acquire_time", None), serialize),
            ("holderIdentity" if serialize else "holder_identity"): _to_legacy_value(getattr(self, "holder_identity", None), serialize),
            ("leaseDurationSeconds" if serialize else "lease_duration_seconds"): _to_legacy_value(getattr(self, "lease_duration_seconds", None), serialize),
            ("leaseTransitions" if serialize else "lease_transitions"): _to_legacy_value(getattr(self, "lease_transitions", None), serialize),
            ("preferredHolder" if serialize else "preferred_holder"): _to_legacy_value(getattr(self, "preferred_holder", None), serialize),
            ("renewTime" if serialize else "renew_time"): _to_legacy_value(getattr(self, "renew_time", None), serialize),
            ("strategy" if serialize else "strategy"): _to_legacy_value(getattr(self, "strategy", None), serialize),
        }

    def __openapi_generator_modern_projection(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    setattr(
        to_dict,
        _OPENAPI_GENERATOR_TO_DICT,
        __openapi_generator_modern_projection,
    )
    setattr(
        __openapi_generator_modern_projection,
        _OPENAPI_GENERATOR_TO_DICT,
        to_dict,
    )
    del __openapi_generator_modern_projection

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of V1LeaseSpec from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        obj = _cast(
            Dict[str, Any],
            cls.__preprocess_input_names(
                obj,
                remove_hidden_storage_names=True,
            ),
        )

        _obj = cls.model_validate({
            "acquireTime": obj.get("acquireTime"),
            "holderIdentity": obj.get("holderIdentity"),
            "leaseDurationSeconds": obj.get("leaseDurationSeconds"),
            "leaseTransitions": obj.get("leaseTransitions"),
            "preferredHolder": obj.get("preferredHolder"),
            "renewTime": obj.get("renewTime"),
            "strategy": obj.get("strategy")
        })
        return _obj
